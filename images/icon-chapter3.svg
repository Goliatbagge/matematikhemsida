<svg width="64" height="64" viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg">
  <!-- Kapitel 3: Derivatan - f' symbol med pil -->

  <!-- f(x) text -->
  <text x="20" y="28" font-size="20" font-family="serif" fill="#2563EB" font-style="italic">f(x)</text>

  <!-- Derivata-pil -->
  <path d="M 32 35 L 32 48" stroke="#F59E0B" stroke-width="2.5" fill="none" marker-end="url(#arrowhead)"/>

  <!-- f'(x) text -->
  <text x="18" y="58" font-size="20" font-family="serif" fill="#2563EB" font-style="italic">f'(x)</text>

  <!-- Arrow marker definition -->
  <defs>
    <marker id="arrowhead" markerWidth="10" markerHeight="10" refX="5" refY="5" orient="auto">
      <polygon points="0 0, 10 5, 0 10" fill="#F59E0B"/>
    </marker>
  </defs>
</svg>